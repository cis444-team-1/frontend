import clsx from "clsx";
import styles from "./button.module.css";
import "./button.module.css";

const Button = () => {
  return <button className={clsx(styles.example)}>Button</button>;
};

export default Button;
